""" A set of functions that implement the 4 simplifications rules of CNF sentences
in order to remove redudant cases from the knowledge base."""

def simplification1(sample_obj):
    if isinstance(sample_obj, tuple):
        print("todo")
    elif isinstance(sample_obj, str):
        return sample_obj
    else:
        #wrong format case
        return None
def simplification2(sample_obj):
    if isinstance(sample_obj, tuple):
        print("todo")
    elif isinstance(sample_obj, str):
        return sample_obj
    else:
        #wrong format case
        return None

def simplification3(sample_obj):
    if isinstance(sample_obj, tuple):
        print("todo")
    elif isinstance(sample_obj, str):
        return sample_obj
    else:
        #wrong format case
        return None

def simplification4(sample_obj):
    if isinstance(sample_obj, tuple):
        print("todo")
    elif isinstance(sample_obj, str):
        return sample_obj
    else:
        #wrong format case
        return None
